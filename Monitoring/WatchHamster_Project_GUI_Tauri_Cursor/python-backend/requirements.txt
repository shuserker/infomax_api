# WatchHamster Cursor Backend Dependencies
# 기존 WatchHamster_Project의 모든 의존성 포함

# FastAPI 및 웹 서버
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Pydantic 호환성 문제 해결 - 최신 버전 사용
pydantic>=2.0.1,<3.0.0
pydantic-settings>=2.0.3

# 시스템 모니터링 (기존 WatchHamster_Project에서 사용)
psutil==5.9.6

# HTTP 클라이언트 (INFOMAX API 연동용)
requests==2.31.0
httpx==0.25.2

# WebSocket 통신
websockets==12.0

# 환경 설정
python-dotenv==1.0.0

# 날짜/시간 처리 (영업일 계산용)
python-dateutil==2.8.2

# JSON 처리 (기존 로직 호환성)
jsonschema==4.19.2

# 로깅 및 디버깅
colorlog==6.7.0

# 비동기 처리
asyncio-mqtt==0.13.0

# 데이터 검증
cerberus==1.3.4

# 파일 처리
pathlib2==2.3.7

# 네트워크 유틸리티
urllib3==2.0.7