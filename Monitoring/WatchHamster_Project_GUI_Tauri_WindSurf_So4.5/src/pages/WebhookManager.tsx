import React from 'react';
import { WebhookManager as WebhookManagerComponent } from '@/components/WebhookManager';

const WebhookManager: React.FC = () => {
  return <WebhookManagerComponent />;
};

export default WebhookManager;
